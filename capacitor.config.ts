import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.rssec.thaicab',
  appName: 'Thai Trainer',
  webDir: 'web',
  plugins: {
    // TextToSpeech plugin config — no extra keys needed; defaults are fine.
  },
};

export default config;
